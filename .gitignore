__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store

# Real business data - never commit actual exports/reports, even to a
# private repo. Only source code should live in git.
*.csv
*.xlsx
*.xls
Steelfort_Reorder_List.xlsx

# Login password - never commit the real secrets file. secrets.toml.example
# is fine to commit since it only has a placeholder.
.streamlit/secrets.toml
